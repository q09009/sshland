# sshland에 기여하기

버그 수정, 문서 개선, 번역, 접근성 개선과 새로운 아이디어를 환영합니다. 프로젝트는 가볍고 초보자에게 이해하기 쉬운 SSH/SFTP 도구를 목표로 합니다.

## 시작하기 전에

- 일반 버그와 기능 제안은 GitHub Issue에 등록해주세요.
- 큰 기능이나 구조 변경은 구현 전에 이슈에서 범위와 방향을 먼저 논의해주세요.
- 보안 문제는 공개 이슈에 올리지 말고 [SECURITY.md](SECURITY.md)를 따라주세요.
- 실제 서버 주소, 사용자명, 로그의 IP, 비밀번호, 개인키와 토큰을 이슈·스크린샷·테스트 데이터에 포함하지 마세요.

## 개발 환경

필요한 도구와 운영체제별 Tauri 의존성은 [README의 설치 준비](README.md#설치-준비)를 참고해주세요.

```bash
git clone https://github.com/q09009/sshland.git
cd sshland
npm ci
npm run tauri dev
```

`npm run dev`는 프런트엔드만 실행합니다. SSH, SFTP와 네이티브 파일 선택 기능을 확인하려면 `npm run tauri dev`를 사용해야 합니다.

## 변경 원칙

- 새 의존성을 추가하기 전에 기존 코드와 가벼운 대안으로 해결할 수 있는지 검토합니다.
- 사용자에게 표시하는 오류는 내부 구현 용어 대신 이해하기 쉬운 문장으로 작성하고 한국어·영어 번역을 함께 갱신합니다.
- 비밀번호와 개인키 암호를 저장하거나 로그에 남기지 않습니다.
- 사용자 설정은 선언적인 데이터로만 처리하며 JavaScript 같은 코드를 설정으로 실행하지 않습니다.
- 파싱에 실패한 명령 결과는 오류 화면으로 막기보다 원본 텍스트 보기로 자연스럽게 돌아갈 수 있어야 합니다.
- 삭제, 덮어쓰기, 프로세스 종료 같은 위험한 동작에는 사용자에게 결과가 분명하게 보이도록 합니다.
- 기존 디자인 토큰으로 표현할 수 있는 공통 색상·간격·글꼴 값을 컴포넌트에 중복해 추가하지 않습니다.

## 제출 전 검사

프런트엔드 빌드:

```bash
npm run build
```

Rust 검사:

```bash
cd src-tauri
cargo fmt --all -- --check
cargo clippy --all-targets --all-features -- -D warnings
cargo test --all-targets --all-features
```

Windows에서 전체 Tauri 실행 파일 빌드를 확인하려면 저장소 루트에서 다음을 실행합니다.

```bash
npm run tauri -- build --no-bundle
```

운영체제와 실제 SSH 서버가 필요한 동작은 자동 검사만으로 충분하지 않습니다. 관련 변경을 했다면 확인한 환경과 시나리오를 pull request에 적어주세요.

파일관리자 동작을 바꿨다면 가능하면 목록·자세히·큰 아이콘 보기에서 다음 항목도 수동으로 확인해주세요.

- 클릭, `Ctrl`/`Command`, `Shift`, 선택 사각형과 `Ctrl+A` 다중 선택
- 현재 폴더 검색 중 선택·열기·메뉴 동작
- 현재 폴더 필터와 `fd`·`find` 재귀 검색, 숨김파일 제외, 설정 옵션 클릭 시 명령 확인, 재접속 시 서버별 확인 결과 복원과 미설치 시 선택 유지
- 파일과 빈 폴더가 섞인 폴더의 재귀 업로드·다운로드
- 다중 복사·이동·삭제와 서로 다른 파일관리자 pane 사이의 드래그 이동
- 같은 이름 충돌, 권한 오류와 폴더를 자기 하위로 복사·이동하려는 경우

## Pull request

- 한 pull request에는 가능한 한 하나의 논리적인 변경만 담아주세요.
- 사용자 동작이 바뀌면 README와 CHANGELOG도 함께 갱신해주세요.
- UI 변경에는 민감 정보를 제거한 전후 스크린샷을 첨부해주세요.
- 새 기능에는 가능한 범위의 자동 테스트를 추가해주세요.
- CI가 통과하지 않으면 원인과 아직 확인하지 못한 항목을 설명해주세요.

기여자가 제출한 코드는 저장소의 [MIT License](LICENSE)에 따라 배포될 수 있다는 데 동의한 것으로 간주합니다.
