# 보안 정책

sshland는 SSH 자격 증명과 원격 서버 파일을 다루므로 보안 문제를 일반 버그보다 신중하게 취급합니다.

## 지원 버전

현재 공개 릴리스 전의 `0.1.x` 개발 버전만 유지보수합니다. 아직 안정 버전 호환성이나 장기 지원을 보장하지 않습니다.

| 버전 | 보안 수정 |
| --- | --- |
| 최신 `main`의 `0.1.x` 개발 버전 | 최선 노력으로 지원 |
| 오래된 커밋, fork와 수정된 빌드 | 지원을 보장하지 않음 |

## 취약점 제보

보안 취약점의 세부 내용, 재현 코드, 실제 서버 주소나 자격 증명을 공개 GitHub Issue에 올리지 마세요.

1. 저장소의 **Security** 탭에서 **Report a vulnerability**를 선택해 비공개로 제보해주세요.
2. 비공개 제보 기능이 보이지 않으면 민감한 세부 내용은 제외하고 공개 이슈로 “비공개 보안 연락 경로가 필요하다”고 알려주세요.
3. 재현에 필요한 최소 정보만 포함하고 비밀번호, 개인키, 토큰, 실제 서버 주소와 개인 데이터는 제거해주세요.

제보에는 가능하면 다음 내용을 포함해주세요.

- 영향을 받는 커밋 또는 버전
- 운영체제와 원격 서버 환경
- 예상한 안전한 동작과 실제 동작
- 민감 정보를 제거한 재현 절차
- 예상되는 영향과 가능한 완화 방법

개인 프로젝트이므로 공식 응답 시간이나 수정 기한을 보장하지는 않지만, 확인한 제보에는 가능한 한 빠르게 접수 여부와 다음 단계를 안내하겠습니다. 수정이 공개되기 전까지는 세부 내용을 비공개로 유지해주세요.

## 우선 확인하는 문제

- 비밀번호, 개인키 암호 또는 기타 비밀 정보의 저장·로그·노출
- SSH 호스트 키 검증 우회 또는 잘못된 서버 키 승인
- 사용자의 명시적인 동작 없이 발생하는 원격 명령 실행
- SFTP 경로 검증 문제로 의도하지 않은 파일을 읽거나 변경하는 동작
- Tauri IPC, CSP 또는 앱 전용 파일 접근 범위의 우회
- 악의적인 서버 응답이나 설정 파일로 인한 코드 실행

## 보안 취약점으로 보지 않는 동작

- 사용자가 터미널, 매크로 또는 사용자 정의 위젯에서 직접 실행한 명령의 결과
- 최초 접속 시 사용자가 확인하지 않고 잘못된 호스트 키 지문을 승인한 경우
- 원격 서버에 필요한 Linux 명령이나 Docker가 없어 해당 위젯을 사용할 수 없는 경우
- 지원 범위 밖의 운영체제 또는 비공식 빌드에서만 발생하는 호환성 문제

일반적인 버그와 개선 제안은 GitHub Issues를 이용해주세요.
